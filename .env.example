PORT=8050
ENVIRONMENT=LOCAL

VAULT_ADDR=https://vault-fpms-aliyun-qat.platform88.me
VAULT_TOKEN=hvs.xxxxxxxxxxxxxxxxxxxxxxxx

# KV v2 path: <mount>/data/<path>, e.g. secret/data/fund-pool-api/local
# The secrets at this path are loaded into process.env at startup.
# Required secrets: MONGODB_URI
# Future: REDIS_URI, third-party URLs, etc.
VAULT_SECRET_PATH=secret/data/dosGameFundPool/qat
